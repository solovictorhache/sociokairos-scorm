[package]
name = "sociokairos-edu"
version = "1.0.0"
description = "SOCIOKAIROS EDU — Research Suite (app de escritorio Mac/Windows)"
authors = ["Víctor Hugo Pérez Gallo — Universidad de Zaragoza"]
edition = "2021"
rust-version = "1.77"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
